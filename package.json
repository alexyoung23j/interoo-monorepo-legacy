{
  "scripts": {
    "start-dev": "cd prisma && npx prisma generate && cd ../idi-app && pnpm dev",
    "build-app": "cd prisma && npx prisma generate && cd ../idi-app && pnpm build",
    "start-app": "cd ../idi-app && pnpm start"
  }
}
